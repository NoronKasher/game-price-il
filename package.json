{
  "name": "game-price-il",
  "private": true,
  "version": "0.1.0",
  "description": "VGPT.IL (Video Games Price Tracker) — Israeli video game price comparison and wishlist tracker",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -k -n server,web \"npm run dev -w server\" \"npm run dev -w web\"",
    "build": "npm run build -w web",
    "build:demo": "npm run build:demo -w web",
    "demo:capture": "node web/demo/capture.mjs",
    "start": "npm run start -w server",
    "test": "npm run test -w server",
    "build:ext": "npm run build:ext -w web && vite build -c extension/vite.config.ts && vite build -c extension/vite.bridge.config.ts && node extension/pack.mjs",
    "demo:verify": "node web/demo/verify.mjs"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.7",
    "concurrently": "^9.1.0"
  }
}
