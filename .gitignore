# Secrets — API keys must never be committed (treat like passwords)
.gg_deals_key
.itad_key
*.key
.env
.env.*

# Local user data — price history is personal and machine-local
data/
*.db
*.db-journal
*.sqlite

# Dependencies & build output
node_modules/
dist/
build/
*.tsbuildinfo

# Editor / OS noise
.DS_Store
Thumbs.db
.vscode/
.idea/

# Demo build output (the snapshot itself IS committed, the build is not)
dist-demo/

# Extension build output (loaded unpacked from here)
extension/dist/

# Bundled server output (built by npm run build:server)
server/dist/
desktop/icon.png
desktop/icon-256.png
extension/*.zip
dist_electron/
desktop/patch-notes.html
